




































{# {% extends 'base.html.twig' %}

{% block title %}Branche{% endblock %}

{% block body %}
    <h1>Branche</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ branche.id }}</td>
            </tr>
            <tr>
                <th>Nom_branche</th>
                <td>{{ branche.NomBranche }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ branche.description }}</td>
            </tr>
            <tr>
                <th>Responsable</th>
                <td>{{ branche.Responsable }}</td>
            </tr>
            <tr>
                <th>Liste_membres</th>
                <td>{{ branche.ListeMembres }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ branche.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('branche_index') }}">back to list</a>

    <a href="{{ path('branche_edit', {'id': branche.id}) }}">edit</a>

    {{ include('branche/_delete_form.html.twig') }}
{% endblock %} #}
